name: Enrich master facilities (no API)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 1 * *"  # 毎月1日 12:00 JST（UTC 03:00）

permissions:
  contents: write

jobs:
  enrich:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Enrich master_facilities.csv
        env:
          WARD_FILTER: 港北区
          WALK_SPEED_M_PER_MIN: "80"
          NOMINATIM_SLEEP_SEC: "1.1"
          NOMINATIM_RETRY: "3"
        run: |
          python -u scripts/enrich_master_facilities.py
          ls -la data | sed -n '1,120p'

      - name: Commit and push
        run: |
          set -euxo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/master_facilities.csv data/geocode_cache.json data/geocode_misses.csv || true
          git commit -m "Enrich master facilities (no API)" || echo "No changes"
          git push
