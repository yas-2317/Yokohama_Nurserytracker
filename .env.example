# ===================================================
# 環境変数テンプレート
# このファイルをコピーして .env を作成してください
# .env は .gitignore に追加して絶対にコミットしないこと
# ===================================================

# Google Places API キー（fix_master_with_google_places.py で使用）
# GitHub Secrets には GOOGLE_API_KEY という名前で登録してください
# 取得先: https://console.cloud.google.com/apis/credentials
GOOGLE_API_KEY=your_google_places_api_key_here

# 対象区フィルター（空欄 = 横浜市全域）
# 例: 港北区, 鶴見区, 神奈川区, 西区, 中区 ...
WARD_FILTER=港北区

# 強制上書きフラグ（backfill_last_year.py 用）
# 0 = 既存JSONをスキップ, 1 = 既存JSONも上書き
FORCE_BACKFILL=0

# 最大API更新件数（fix_master_with_google_places.py 用）
# Google Places API の費用を抑えるための上限
MAX_UPDATES=50
